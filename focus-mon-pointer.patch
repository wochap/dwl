From cae5b36f39e277ddbcfab88ceb5846a45f43aa62 Mon Sep 17 00:00:00 2001
From: wochap <gean.marroquin@gmail.com>
Date: Thu, 11 Apr 2024 12:39:21 -0500
Subject: [PATCH] focusmon() moves the cursor accordingly

---
 dwl.c | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/dwl.c b/dwl.c
index bf763df..180369e 100644
--- a/dwl.c
+++ b/dwl.c
@@ -1311,6 +1311,7 @@ focusclient(Client *c, int lift)
 void
 focusmon(const Arg *arg)
 {
+	Monitor *prevm = selmon;
 	int i = 0, nmons = wl_list_length(&mons);
 	if (nmons) {
 		do /* don't switch to disabled mons */
@@ -1318,6 +1319,7 @@ focusmon(const Arg *arg)
 		while (!selmon->wlr_output->enabled && i++ < nmons);
 	}
 	focusclient(focustop(selmon), 1);
+	wlr_cursor_move(cursor, NULL, selmon->m.x - prevm->m.x , 0);
 }
 
 void
-- 
2.43.2

